import { pageMetadata } from '@/lib/seo'
export const metadata = pageMetadata("Shop A-Bar Chocolate & Bundles | BeGood", "Shop BeGood A-Bar functional chocolate as a single 40 g bar, a two-bar Duo or a five-pack. Compare pack sizes, prices and ingredients.", "/shop")
import PageClient from './PageClient'
export default function Page() { return <PageClient /> }

